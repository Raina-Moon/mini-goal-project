"use client";

import LoginForm from "./LoginForm";

const page = () => {
  return (
    <div className="bg-primary-500 min-h-screen font-inter">
      <LoginForm />
    </div>
  );
};

export default page;
